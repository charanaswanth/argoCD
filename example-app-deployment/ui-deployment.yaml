apiVersion: apps/v1
kind: Deployment
metadata:
  name: example-app-ui
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: example-app-ui
  template:
    metadata:
      labels:
        app: example-app-ui
    spec:
      containers:
        - image: gcr.io/google-samples/gb-frontend:v5
          name: example-app-ui
          ports:
            - containerPort: 80
